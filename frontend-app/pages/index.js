import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
    return (
        <Typography variant="h4" component="h1" gutterBottom>
            Welcome to the Property Management App
        </Typography>
    );
};

export default Home;
